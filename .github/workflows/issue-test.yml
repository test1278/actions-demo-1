name: test issues

env:
  GH_TOKEN: ${{ secrets.ABC_TOKEN }}

on:
  issues:

jobs:
  test-issues-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test-issues-Actions 111"
      - run: ./shell.sh


